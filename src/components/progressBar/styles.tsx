import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  track: {
    width: "100%",
    borderRadius: 10,
    overflow: "hidden",
  },
  fill: {
    borderRadius: 10,
  },
});

export default styles;
